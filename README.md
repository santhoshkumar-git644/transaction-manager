# transaction-manager
Built a Transaction Manager in C++ that simulates core database functionalities, including concurrency control, deadlock detection, crash recovery. Implemented Strict Locking (2PL), a wait-for graph for deadlock detection, and write-ahead logging (WAL) to support undo/redo recovery, ensuring full ACID compliance in a multi-transaction environment.
